---
import "../styles/global.css";
import Header from "../components/static/Header.astro";
import Footer from "../components/static/Footer.astro";
import { Toaster } from "../components/shadcn-ui/sonner";

export interface Props {
  title: string;
  description?: string;
  keywords?: string;
  ogTitle?: string;
  ogDescription?: string;
  ogImage?: string;
  canonicalURL?: string;
  structuredData?: object;
  transparentHeader?: boolean;
}

const {
  title,
  description = "岐阜県中津川市のどんぐりピアノ教室。初心者から上級者まで、一人ひとりに合わせた丁寧な指導で、楽しくピアノを学べます。無料体験レッスン受付中。",
  keywords = "ピアノ教室,中津川市,岐阜県,ピアノレッスン,音楽教室,どんぐりピアノ教室,初心者,子供,大人",
  ogTitle,
  ogDescription,
  ogImage = "/background.png",
  canonicalURL,
  structuredData,
  transparentHeader = false,
} = Astro.props;

const siteURL = Astro.site || "https://donguri-piano.com";
const pageURL = canonicalURL || new URL(Astro.url.pathname, siteURL).href;
const finalOgTitle = ogTitle || title;
const finalOgDescription = ogDescription || description;
---

<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="/favicon.ico" sizes="32x32" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg?v=2" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <meta name="generator" content={Astro.generator} />
    <meta name="theme-color" content="#faf6ef" />

    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="keywords" content={keywords} />
    <link rel="canonical" href={pageURL} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Zen+Kaku+Gothic+New:wght@400;500;700&family=Zen+Maru+Gothic:wght@500;700&display=swap"
      rel="stylesheet"
    />

    <meta property="og:title" content={finalOgTitle} />
    <meta property="og:description" content={finalOgDescription} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={pageURL} />
    <meta property="og:image" content={new URL(ogImage, siteURL).href} />
    <meta property="og:locale" content="ja_JP" />
    <meta property="og:site_name" content="どんぐりピアノ教室" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={finalOgTitle} />
    <meta name="twitter:description" content={finalOgDescription} />
    <meta name="twitter:image" content={new URL(ogImage, siteURL).href} />

    {
      structuredData && (
        <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
      )
    }
  </head>
  <body class="min-h-screen flex flex-col">
    <Header overlay={transparentHeader} />
    <main class="flex-grow">
      <slot />
    </main>
    <Footer />
    <Toaster client:load />
  </body>
</html>
